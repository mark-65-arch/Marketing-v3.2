# 🔐 Environment Variables for Marketing AI Houston
# Copy this file to .env and fill in your actual values
# ⚠️ NEVER commit .env files to version control

# 📊 Google Tag Manager Configuration
# Get your GTM container ID from https://tagmanager.google.com
# Format: GTM-XXXXXXX
PUBLIC_GTM_ID=GTM-KQS29VV6

# 📈 Google Analytics 4 Configuration
# Get your GA4 measurement ID from https://analytics.google.com
# Format: G-XXXXXXXXXX
PUBLIC_GA4_ID=

# 🔑 API Keys (if needed in future)
# Never expose private API keys in client-side code (no PUBLIC_ prefix)
# Examples:
# STRIPE_SECRET_KEY=sk_test_...
# RESEND_API_KEY=re_...
# DATABASE_URL=postgresql://...

# ℹ️ Security Notes:
# - The PUBLIC_ prefix makes variables available in client-side code
# - Never use PUBLIC_ prefix for sensitive data (API keys, secrets, credentials)
# - All sensitive variables should be server-side only (no PUBLIC_ prefix)
# - Ensure .env files are in .gitignore
# - Rotate keys immediately if accidentally committed
# - Use different credentials for development and production

# ℹ️ Analytics Notes:
# - If PUBLIC_GTM_ID is not set, the default GTM-KQS29VV6 will be used
# - If PUBLIC_GA4_ID is not set, GA4 will not be initialized
# - Both analytics tools respect user cookie consent preferences
# - Analytics is enabled by default but users can opt-out anytime
