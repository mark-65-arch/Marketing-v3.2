---
// 🎯 Get Found on Google Service Page
// Purpose: Service-specific landing page for Google Business Profile optimization
// Features: Service details, benefits, pricing, FAQ, conversion optimization

import MainLayout from '../layouts/MainLayout.astro';
import Card from '../components/ui/Card.astro';
import Button from '../components/ui/Button.astro';

import {
  generateServiceSchema,
  generateFAQSchema,
  type BreadcrumbItem,
  type FAQItem
} from '../utils/seo.ts';
import BroomIcon from '../components/icons/BroomIcon.astro';
import CameraIcon from '../components/icons/CameraIcon.astro';
import CarIcon from '../components/icons/CarIcon.astro';
import CheckCircleIcon from '../components/icons/CheckCircleIcon.astro';
import CheckIcon from '../components/icons/CheckIcon.astro';
import ChevronDownIcon from '../components/icons/ChevronDownIcon.astro';
import CommentsIcon from '../components/icons/CommentsIcon.astro';
import DollarIcon from '../components/icons/DollarIcon.astro';
import HammerIcon from '../components/icons/HammerIcon.astro';
import LeafIcon from '../components/icons/LeafIcon.astro';
import MagicIcon from '../components/icons/MagicIcon.astro';
import PawIcon from '../components/icons/PawIcon.astro';
import QuestionCircleIcon from '../components/icons/QuestionCircleIcon.astro';
import RouteIcon from '../components/icons/RouteIcon.astro';
import StarIcon from '../components/icons/StarIcon.astro';
import TrophyIcon from '../components/icons/TrophyIcon.astro';

// 🔍 SEO Configuration
const pageTitle = 'Google Business Profile Optimization — Local SEO & Maps Visibility';
const pageDescription = 'We optimize your profile so you show up when customers search — on Maps, mobile, and voice. Get found by local customers actively looking for your services.';
const canonicalURL = `${import.meta.env.SITE}/business-profile-optimization`;

// 🍞 Breadcrumb Navigation
const breadcrumbs: BreadcrumbItem[] = [
  { name: 'Home', url: import.meta.env.BASE_URL },
  { name: 'Services', url: `${import.meta.env.BASE_URL}#services` },
  { name: 'Get Found on Google (Starter Plan)', url: `${import.meta.env.BASE_URL}business-profile-optimization` }
];

// 📊 Service Schema Data
const serviceSchema = generateServiceSchema({
  name: 'Google Business Profile Optimization',
  description: 'Professional Google Business Profile optimization to help local businesses get found on Google Maps, mobile search, and voice search.',
  provider: 'Marketing AI Houston',
  serviceType: 'Local SEO',
  areaServed: 'United States',
  priceRange: '$497'
});

// ❓ FAQ Data
const faqs: FAQItem[] = [
  {
    question: 'Do I need a website?',
    answer: 'No, this service works even without one. Your Google Business Profile becomes your digital storefront.'
  },
  {
    question: 'Will I show up on Maps?',
    answer: 'Yes — we optimize your profile for maximum local visibility on Google Maps.'
  },
  {
    question: 'Can I manage reviews myself?',
    answer: 'Absolutely. We train you and provide templates to make it easy.'
  },
  {
    question: 'Is this a one-time service?',
    answer: 'Yes, but ongoing support plans are available if you want continued optimization.'
  },
  {
    question: 'Do you only work with Houston businesses?',
    answer: 'Not at all. We\'re proudly based in Houston, but we serve small business owners across the U.S. Our process is fully remote-friendly.'
  }
];

const faqSchema = generateFAQSchema(faqs);
---

<MainLayout
  title={pageTitle}
  description={pageDescription}
  canonicalURL={canonicalURL}
  breadcrumbs={breadcrumbs}
  structuredData={[serviceSchema, faqSchema]}
  pageType="service"
  currentPath="/get-found-on-google"
>
  <!-- 🎯 Hero Section -->
  <section class="section-padding relative overflow-hidden">
    <!-- 🎨 Background Elements -->
    <!-- Commented out - floating blobs disabled (can be re-enabled if needed)
    <div class="absolute inset-0 opacity-10">
      <div class="absolute top-20 left-20 w-64 h-64 bg-gradient-to-br from-blue-600 to-teal-600 organic-blob animate-float"></div>
      <div class="absolute bottom-20 right-20 w-72 h-72 bg-gradient-to-br from-teal-600 to-blue-300 organic-leaf animate-float animation-delay-400"></div>
    </div>
    -->

    <div class="container mx-auto container-padding relative z-10">
      <div class="text-center max-w-4xl mx-auto">
        <!-- ⭐ Trust Signal -->
        <div class="text-yellow-400 text-3xl mb-4">⭐⭐⭐⭐⭐</div>
        <p class="text-gray-700 mb-8 font-medium">Trusted by Small Business Owners Nationwide</p>

        <!-- 🎯 Main Headline -->
        <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-black text-gray-900 leading-tight mb-6">
          <span class="text-gradient font-black block mb-4 leading-tight" style="padding-bottom: 0.25rem;">Google Business Profile Optimization</span>
        </h1>

        <!-- 📝 Subheadline -->
        <p class="text-xl text-gray-700 mb-12 max-w-3xl mx-auto leading-relaxed">
          We optimize your profile so you show up when customers search — on Maps, mobile, and voice.
        </p>

        <!-- 🎯 Primary CTA -->
        <Button
          variant="primary"
          size="lg"
          href={`${import.meta.env.BASE_URL}contact#contact-options`}
          class="mb-6"
        >
          <span class="hidden sm:inline">👉 Book a <span class="bg-yellow-400 text-black px-2 py-1 rounded font-bold">FREE</span> Discovery Call</span>
          <span class="sm:hidden">Book <span class="bg-yellow-400 text-black px-2 py-1 rounded font-bold">FREE</span> Call</span>
        </Button>

        <!-- 💬 Supporting Text -->
        <p class="text-gray-700 mb-16">No pressure, no jargon — just a quick chat to see if we're a fit.</p>

        <!-- 📊 Key Metrics -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
          <Card class="text-center" padding="lg">
            <div class="text-4xl mb-3">🗺️</div>
            <div class="text-gray-900 font-bold">Show up on Maps</div>
            <div class="text-gray-700 text-sm">when customers search locally</div>
          </Card>

          <Card class="text-center" padding="lg">
            <div class="text-4xl mb-3">📱</div>
            <div class="text-gray-900 font-bold">Mobile-optimized</div>
            <div class="text-gray-700 text-sm">for on-the-go customers</div>
          </Card>

          <Card class="text-center" padding="lg">
            <div class="text-4xl mb-3">🎤</div>
            <div class="text-gray-900 font-bold">Voice search ready</div>
            <div class="text-gray-700 text-sm">for "near me" searches</div>
          </Card>
        </div>

        <!-- 🏢 Industry Icons -->
        <div class="mt-16 flex flex-wrap justify-center gap-8">
          <div class="flex flex-col items-center">
            <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg mb-3">
              <HammerIcon class="text-2xl text-gray-900" aria-hidden="true" />
            </div>
            <span class="text-gray-700 font-medium">Contractors</span>
          </div>
          <div class="flex flex-col items-center">
            <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg mb-3">
              <CarIcon class="text-2xl text-gray-900" aria-hidden="true" />
            </div>
            <span class="text-gray-700 font-medium">Auto Shops</span>
          </div>
          <div class="flex flex-col items-center">
            <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg mb-3">
              <BroomIcon class="text-2xl text-gray-900" aria-hidden="true" />
            </div>
            <span class="text-gray-700 font-medium">Cleaning</span>
          </div>
          <div class="flex flex-col items-center">
            <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg mb-3">
              <LeafIcon class="text-2xl text-gray-900" aria-hidden="true" />
            </div>
            <span class="text-gray-700 font-medium">Landscaping</span>
          </div>
          <div class="flex flex-col items-center">
            <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg mb-3">
              <PawIcon class="text-2xl text-gray-900" aria-hidden="true" />
            </div>
            <span class="text-gray-700 font-medium">Pet Services</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 📋 What You Get Section -->
  <section class="section-padding relative overflow-hidden bg-gradient-to-br from-gray-50 to-white">
    <div class="container mx-auto container-padding">
      <div class="text-center mb-16">
        <div class="inline-flex items-center bg-white/80 backdrop-blur-sm px-6 py-3 rounded-full text-sm font-semibold text-gray-700 mb-6 border border-white/50 shadow-sm">
          <CheckCircleIcon class="mr-2 text-blue-600" aria-hidden="true" />
          What You Get
        </div>
        <h2 class="heading-lg mb-4">
          <span class="text-gradient">Everything You Need to Get Found</span>
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <Card class="flex items-start space-x-4" padding="lg">
          <div class="flex-shrink-0">
            <MagicIcon class="text-2xl text-blue-600" aria-hidden="true" />
          </div>
          <div>
            <h3 class="text-xl font-bold text-blue-900 mb-2">Complete Profile Makeover</h3>
            <p class="text-gray-700">Professional setup with accurate business info, categories, and optimization for maximum visibility.</p>
          </div>
        </Card>

        <Card class="flex items-start space-x-4" padding="lg">
          <div class="flex-shrink-0">
            <CameraIcon class="text-2xl text-blue-600" aria-hidden="true" />
          </div>
          <div>
            <h3 class="text-xl font-bold text-blue-900 mb-2">Photo Organization</h3>
            <p class="text-gray-700">We organize your existing photos to showcase your business in the best light and improve click-through rates.</p>
          </div>
        </Card>

        <Card class="flex items-start space-x-4" padding="lg">
          <div class="flex-shrink-0">
            <StarIcon class="text-2xl text-blue-600" aria-hidden="true" />
          </div>
          <div>
            <h3 class="text-xl font-bold text-blue-900 mb-2">Review Request System</h3>
            <p class="text-gray-700">Templates and training to help you collect positive reviews from happy customers consistently.</p>
          </div>
        </Card>

        <Card class="flex items-start space-x-4" padding="lg">
          <div class="flex-shrink-0">
            <CommentsIcon class="text-2xl text-blue-600" aria-hidden="true" />
          </div>
          <div>
            <h3 class="text-xl font-bold text-blue-900 mb-2">Messaging Setup</h3>
            <p class="text-gray-700">Auto-reply messages and quick responses to help you connect with potential customers instantly.</p>
          </div>
        </Card>
      </div>
    </div>
  </section>

  <!-- 💎 Benefits Section -->
  <section class="section-padding relative overflow-hidden">
    <div class="absolute inset-0 opacity-5">
      <div class="absolute top-0 right-0 w-96 h-96 bg-teal-600 rounded-full filter blur-3xl"></div>
    </div>

    <div class="container mx-auto container-padding relative z-10">
      <div class="text-center mb-16">
        <div class="inline-flex items-center bg-white/80 backdrop-blur-sm px-6 py-3 rounded-full text-sm font-semibold text-gray-700 mb-6 border border-white/50 shadow-sm">
          <TrophyIcon class="mr-2 text-teal-600" aria-hidden="true" />
          The Results
        </div>
        <h2 class="heading-lg mb-4">
          <span class="text-gradient">What Houston Business Owners Get</span>
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <Card class="text-center" padding="lg">
          <div class="text-5xl mb-4">📍</div>
          <h3 class="text-xl font-bold text-blue-900 mb-3">Show Up Locally</h3>
          <p class="text-gray-700">Appear when customers search for your services in your area.</p>
        </Card>

        <Card class="text-center" padding="lg">
          <div class="text-5xl mb-4">📞</div>
          <h3 class="text-xl font-bold text-blue-900 mb-3">More Phone Calls</h3>
          <p class="text-gray-700">Turn your profile into a phone-ringing machine.</p>
        </Card>

        <Card class="text-center" padding="lg">
          <div class="text-5xl mb-4">🎯</div>
          <h3 class="text-xl font-bold text-blue-900 mb-3">Target Ready Customers</h3>
          <p class="text-gray-700">Reach people actively looking for what you offer.</p>
        </Card>

        <Card class="text-center" padding="lg">
          <div class="text-5xl mb-4">⚡</div>
          <h3 class="text-xl font-bold text-blue-900 mb-3">Quick Setup</h3>
          <p class="text-gray-700">Get results in days, not months.</p>
        </Card>
      </div>
    </div>
  </section>

  <!-- 💡 Why It Works Section -->
  <section class="section-padding bg-gradient-to-br from-blue-50 to-white">
    <div class="container mx-auto container-padding">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="heading-lg mb-8">
          <span class="text-gradient">Your Most Powerful Local Marketing Tool</span>
        </h2>
        <div class="text-xl text-gray-800 leading-relaxed space-y-6">
          <p>
            Old agencies ignore your Google Business Profile. We make it your most powerful local marketing tool — optimized for how people search today. Whether it's Maps, mobile, or voice, we help you show up where it counts.
          </p>
          <p>
            Our approach focuses on accurate data, mobile usability, and conversion-first messaging.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- 🛤️ How It Works Section -->
  <section class="section-padding relative overflow-hidden">
    <!-- Commented out - floating blobs disabled (can be re-enabled if needed)
    <div class="absolute inset-0 opacity-10">
      <div class="absolute top-20 left-20 w-64 h-64 bg-gradient-to-br from-blue-600 to-teal-600 organic-droplet animate-float"></div>
    </div>
    -->

    <div class="container mx-auto container-padding relative z-10">
      <div class="text-center mb-16">
        <div class="inline-flex items-center bg-white/80 backdrop-blur-sm px-6 py-3 rounded-full text-sm font-semibold text-gray-700 mb-6 border border-white/50 shadow-sm">
          <RouteIcon class="mr-2 text-blue-600" aria-hidden="true" />
          Simple Process
        </div>
        <h2 class="heading-lg mb-4">
          <span class="text-gradient">Getting Started Is Simple</span>
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <Card class="text-center" padding="xl">
          <div class="w-20 h-20 rounded-full gradient-ocean-teal flex items-center justify-center mx-auto mb-6 shadow-lg">
            <span class="text-white text-2xl font-bold">1</span>
          </div>
          <div class="text-5xl mb-4">💬</div>
          <h3 class="text-2xl font-bold text-blue-900 mb-4">Discovery Call</h3>
          <p class="text-gray-700">We discuss your business, location, and goals. Takes about 15 minutes.</p>
        </Card>

        <Card class="text-center" padding="xl">
          <div class="w-20 h-20 rounded-full gradient-ocean-teal flex items-center justify-center mx-auto mb-6 shadow-lg">
            <span class="text-white text-2xl font-bold">2</span>
          </div>
          <div class="text-5xl mb-4">⚙️</div>
          <h3 class="text-2xl font-bold text-blue-900 mb-4">Profile Optimization</h3>
          <p class="text-gray-700">We optimize every element for maximum local visibility and customer engagement.</p>
        </Card>

        <Card class="text-center" padding="xl">
          <div class="w-20 h-20 rounded-full gradient-ocean-teal flex items-center justify-center mx-auto mb-6 shadow-lg">
            <span class="text-white text-2xl font-bold">3</span>
          </div>
          <div class="text-5xl mb-4">🚀</div>
          <h3 class="text-2xl font-bold text-blue-900 mb-4">Go Live</h3>
          <p class="text-gray-700">Your optimized profile goes live and starts attracting local customers immediately.</p>
        </Card>
      </div>
    </div>
  </section>

  <!-- 💰 Pricing Section -->
  <section class="section-padding bg-gradient-to-br from-gray-50 to-white">
    <div class="container mx-auto container-padding">
      <div class="text-center mb-16">
        <div class="inline-flex items-center bg-white/80 backdrop-blur-sm px-6 py-3 rounded-full text-sm font-semibold text-gray-700 mb-6 border border-white/50 shadow-sm">
          <DollarIcon class="mr-2 text-teal-600" aria-hidden="true" />
          Simple Pricing
        </div>
        <h2 class="heading-lg mb-4">
          <span class="text-gradient">One Service, One Price</span>
        </h2>
      </div>

      <div class="max-w-lg mx-auto">
        <Card variant="pricing" class="text-center transform hover:scale-105 transition-transform duration-300">
          <div class="p-8">
            <div class="text-6xl mb-6">🎯</div>
            <h3 class="text-3xl font-bold text-blue-900 mb-4">Get Found on Google</h3>
            <p class="text-gray-600 mb-6">Perfect for businesses who need to show up on Google and start getting calls.</p>

            <div class="text-5xl font-black text-blue-900 mb-8">$497</div>

            <div class="text-left space-y-4 mb-8">
              <div class="flex items-start">
                <CheckIcon class="text-blue-600 mt-1 mr-3" aria-hidden="true" />
                <span>Google Business Profile makeover & optimization</span>
              </div>
              <div class="flex items-start">
                <CheckIcon class="text-blue-600 mt-1 mr-3" aria-hidden="true" />
                <span>Accurate business info + categories</span>
              </div>
              <div class="flex items-start">
                <CheckIcon class="text-blue-600 mt-1 mr-3" aria-hidden="true" />
                <span>Photo organization using your existing photos</span>
              </div>
              <div class="flex items-start">
                <CheckIcon class="text-blue-600 mt-1 mr-3" aria-hidden="true" />
                <span>Review request templates & training</span>
              </div>
              <div class="flex items-start">
                <CheckIcon class="text-blue-600 mt-1 mr-3" aria-hidden="true" />
                <span>Messaging setup with auto-replies</span>
              </div>
              <div class="flex items-start">
                <CheckIcon class="text-blue-600 mt-1 mr-3" aria-hidden="true" />
                <span>Voice search formatting</span>
              </div>
            </div>

            <Button
              variant="primary"
              size="lg"
              href={`${import.meta.env.BASE_URL}contact#contact-options`}
              fullWidth
            >
              <span class="hidden sm:inline">Book a <span class="bg-yellow-400 text-black px-2 py-1 rounded font-bold ml-1">FREE</span> Discovery Call</span>
              <span class="sm:hidden">Book <span class="bg-yellow-400 text-black px-2 py-1 rounded font-bold">FREE</span> Call</span>
            </Button>
          </div>
        </Card>
      </div>
    </div>
  </section>

  <!-- ❓ FAQ Section -->
  <section class="section-padding relative overflow-hidden">
    <div class="container mx-auto container-padding">
      <div class="text-center mb-16">
        <div class="inline-flex items-center bg-white/80 backdrop-blur-sm px-6 py-3 rounded-full text-sm font-semibold text-gray-700 mb-6 border border-white/50 shadow-sm">
          <QuestionCircleIcon class="mr-2 text-teal-600" aria-hidden="true" />
          Questions
        </div>
        <h2 class="heading-lg mb-4">
          <span class="text-gradient">Frequently Asked Questions</span>
        </h2>
        <p class="text-xl text-gray-700 max-w-2xl mx-auto">Straight answers — no jargon, no hidden fine print.</p>
      </div>

      <div class="max-w-3xl mx-auto space-y-4" itemscope itemtype="https://schema.org/FAQPage">
        {faqs.map((faq, index) => (
          <Card class="overflow-hidden" key={index}>
            <div itemscope itemprop="mainEntity" itemtype="https://schema.org/Question">
              <button class="accordion-trigger w-full text-left p-6 hover:bg-blue-50/30 focus:outline-none flex justify-between items-center">
                <span class="font-bold text-blue-900 flex items-center">
                  <span class="mr-3 text-2xl">
                    {index === 0 ? '🌐' : index === 1 ? '🗺️' : index === 2 ? '⭐' : index === 3 ? '🔄' : '🌍'}
                  </span>
                  <span itemprop="name">{faq.question}</span>
                </span>
                <ChevronDownIcon class="text-blue-600 transition-transform duration-300" aria-hidden="true" />
              </button>
              <div class="accordion-content" itemscope itemprop="acceptedAnswer" itemtype="https://schema.org/Answer">
                <div class="p-6 pt-0 text-gray-700" itemprop="text">
                  {faq.answer}
                </div>
              </div>
            </div>
          </Card>
        ))}
      </div>
    </div>
  </section>

  <!-- 🎯 Final CTA Section -->
  <section class="section-padding bg-gradient-to-br from-blue-900 to-blue-600 text-white">
    <div class="container mx-auto container-padding text-center">
      <h2 class="text-4xl md:text-5xl font-black mb-6">
        Ready to Get Found on Google?
      </h2>
      <p class="text-xl text-blue-100 mb-12 max-w-2xl mx-auto">
        Let's optimize your profile so customers can find you — fast.
      </p>

      <div class="flex flex-col sm:flex-row gap-6 justify-center">
        <Button
          variant="secondary"
          size="lg"
          href={`${import.meta.env.BASE_URL}contact#contact-options`}
        >
          <span class="hidden sm:inline">👉 Schedule a <span class="bg-yellow-400 text-black px-2 py-1 rounded font-bold ml-1">FREE</span> Discovery Call</span>
          <span class="sm:hidden">Get <span class="bg-yellow-400 text-black px-2 py-1 rounded font-bold">FREE</span> Call</span>
        </Button>

        <Button
          variant="outline"
          size="lg"
          href={`${import.meta.env.BASE_URL}contact#contact-form`}
          class="border-white text-white hover:bg-white hover:text-blue-900"
        >
          <span class="hidden sm:inline">📩 Request a Quote</span>
          <span class="sm:hidden">📩 Get Quote</span>
        </Button>
      </div>
    </div>
  </section>
</MainLayout>

<style>
  /* Define CSS custom properties for colors */
  :root {
    --color-ocean-deep: #0A2540;
    --color-ocean-deeper: #020617;
    --color-ocean-blue: #3B82F6;
    --color-ocean-light: #60A5FA;
    --color-ocean-teal: #198072;
    --color-ocean-teal-dark: #14B8A6;
    --color-ocean-sky: #BFDBFE;
  }

  /* Typography */
  h1, h2, h3, h4, h5, h6 {
    font-family: 'Poppins', sans-serif;
  }

  p, a, span, li {
    font-family: 'Inter', sans-serif;
  }

  /* Force maximum boldness for headers */
  h1, h1 *, h1 span {
    font-weight: 900 !important;
    font-family: 'Poppins', sans-serif !important;
  }

  h2, h2 *, h2 span {
    font-weight: 900 !important;
    font-family: 'Poppins', sans-serif !important;
  }

  /* Layout utilities */
  .section-padding {
    padding: 5rem 0;
  }

  .container-padding {
    padding-left: 1rem;
    padding-right: 1rem;
  }

  /* Typography utilities */
  .heading-xl {
    font-size: 3rem;
    font-weight: 900;
    line-height: 1.1;
    font-family: 'Poppins', sans-serif;
  }

  .heading-lg {
    font-size: 2.5rem;
    font-weight: 900;
    line-height: 1.2;
    font-family: 'Poppins', sans-serif;
  }

  @media (min-width: 768px) {
    .section-padding {
      padding: 6rem 0;
    }

    .heading-xl {
      font-size: 4rem;
    }

    .heading-lg {
      font-size: 3rem;
    }
  }

  @media (min-width: 1024px) {
    .section-padding {
      padding: 8rem 0;
    }

    .heading-xl {
      font-size: 5rem;
    }

    .heading-lg {
      font-size: 4rem;
    }
  }

  /* Organic shapes */
  /* Commented out - floating blobs disabled (can be re-enabled if needed)
  .organic-blob {
    border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
  }

  .organic-droplet {
    border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
  }

  .organic-leaf {
    border-radius: 20% 80% 20% 80% / 80% 20% 80% 20%;
  }
  */

  /* Gradients */
  .gradient-ocean-teal {
    background: linear-gradient(135deg, var(--color-ocean-blue), var(--color-ocean-teal));
  }

  /* 📱 Mobile-specific optimizations */
  @media (max-width: 640px) {
    /* Better word wrapping on mobile */
    h1 {
      word-wrap: break-word;
      overflow-wrap: break-word;
      hyphens: auto;
    }

    /* Ensure buttons don't overflow on mobile */
    button, a[role="button"] {
      max-width: 100%;
      white-space: normal;
      text-align: center;
    }

    /* Reduce padding on mobile for better fit */
    .section-padding {
      padding: 3rem 0;
    }
  }

  /* Animations */
  .animation-delay-200 { animation-delay: 0.2s; }
  .animation-delay-400 { animation-delay: 0.4s; }
  .animation-delay-600 { animation-delay: 0.6s; }
  .animation-delay-800 { animation-delay: 0.8s; }

  @keyframes float {
    0% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
    100% { transform: translateY(0px); }
  }

  @keyframes blob {
    0% { transform: translate(0px, 0px) scale(1); }
    33% { transform: translate(30px, -50px) scale(1.1); }
    66% { transform: translate(-20px, 20px) scale(0.9); }
    100% { transform: translate(0px, 0px) scale(1); }
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }

  .animate-blob {
    animation: blob 7s infinite;
  }

  /* 🎯 Service Page Specific Styles */
  .accordion-content {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease-out;
  }

  .accordion-open .accordion-content {
    max-height: 200px;
    transition: max-height 0.5s ease-in;
  }

  /* 🎯 Consistent accordion behavior across all devices */

  /* 🎨 Enhanced hover effects */
  .card-glass:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  }

  /* 🎯 Gradient text animation */
  .text-gradient {
    background: linear-gradient(135deg, #3b82f6, #14b8a6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    color: transparent;
    font-weight: 900 !important;
  }

  @keyframes gradient-shift {
    0% {
      background-position: 0% 50%;
    }
    100% {
      background-position: 100% 50%;
    }
  }
</style>

<!-- 🎯 Service Page Interactions (CSP-compliant, deferred) -->
<script defer src={`${import.meta.env.BASE_URL}js/service-page.js`}></script>