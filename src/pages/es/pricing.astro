---
// 💰 Pricing Page
// Purpose: Clear, simple pricing for small business owners
// Features: Three pricing tiers, comparison table, what's included, commitments, FAQ, CTAs

import MainLayout from '../../layouts/MainLayout.astro';

const breadcrumbs = [
  { name: 'Inicio', url: `${import.meta.env.BASE_URL}es/` },
  { name: 'Precios', url: `${import.meta.env.BASE_URL}es/pricing` }
];

const alternateLanguages = [
  { lang: 'en', url: `${import.meta.env.SITE}${import.meta.env.BASE_URL}pricing` },
  { lang: 'es', url: `${import.meta.env.SITE}${import.meta.env.BASE_URL}es/pricing` }
];

// Pricing data structure
const pricingPlans = [
  {
    id: 'starter',
    name: 'Inicio',
    tagline: 'Sea Encontrado en Línea',
    price: '$497',
    icon: '💡',
    popular: false,
    features: [
      'Renovación y optimización de GBP',
      'Información comercial precisa + categorías',
      'Organización de fotos usando sus fotos existentes',
      'Plantillas de solicitud de reseñas y capacitación',
      'Configuración de mensajería con respuestas automáticas'
    ],
    bestFor: 'Negocios que necesitan aparecer en Google y comenzar a recibir llamadas.',
    learnMoreUrl: `${import.meta.env.BASE_URL}es/business-profile-optimization`,
    learnMoreLabel: 'Sea Encontrado en Google (Plan Inicio)',
    ctaUrl: `${import.meta.env.BASE_URL}es/contact#contact-options`,
    ctaLabel: 'Reserve una GRATIS Consulta de Descubrimiento'
  },
  {
    id: 'growth',
    name: 'Crecimiento',
    tagline: 'Construya Su Base Digital',
    price: '$1,497',
    icon: '🚀',
    popular: true,
    features: [
      'Sitio web de 4 páginas optimizado para móviles',
      'Diseño moderno y responsivo',
      'SEO local + marcado de esquema',
      'Optimización de búsqueda por voz',
      'Hacer clic para llamar y formularios de contacto',
      'Configuración de Google Analytics',
      'Incluye todos los servicios de Inicio'
    ],
    bestFor: 'Propietarios listos para convertir su sitio web en una fuente constante de clientes.',
    learnMoreUrl: `${import.meta.env.BASE_URL}es/website-design-that-converts`,
    learnMoreLabel: 'Construya la Base de Su Sitio Web (Plan Crecimiento)',
    ctaUrl: `${import.meta.env.BASE_URL}es/contact#contact-options`,
    ctaLabel: 'Reserve una GRATIS Consulta de Descubrimiento'
  },
  {
    id: 'pro',
    name: 'Pro',
    tagline: 'Expanda Su Presencia en Línea',
    price: '$3,497',
    icon: '🌐',
    popular: false,
    features: [
      'Sitio web completo de hasta 16 páginas',
      'Páginas de servicios individuales (hasta 6)',
      'Páginas de destino de áreas locales (hasta 6 áreas de Houston)',
      'Creación de contenido personalizado',
      'Seguimiento avanzado de conversiones',
      'Análisis de la competencia + estrategia de palabras clave',
      'Optimización de 90 días con revisiones mensuales',
      'Incluye todos los servicios de Crecimiento'
    ],
    bestFor: 'Negocios listos para dominar su mercado y escalar el crecimiento.',
    learnMoreUrl: `${import.meta.env.BASE_URL}es/pro-website-growth-plan`,
    learnMoreLabel: 'Expanda Su Presencia en Línea (Plan Pro)',
    ctaUrl: `${import.meta.env.BASE_URL}es/contact#contact-options`,
    ctaLabel: 'Reserve una GRATIS Consulta de Descubrimiento'
  }
];

// Comparison table data
const comparisonFeatures = [
  { feature: 'Sitio web optimizado para móviles', starter: '—', growth: '4 páginas', pro: 'Hasta 16 páginas' },
  { feature: 'Páginas de servicios individuales', starter: '—', growth: '—', pro: 'Hasta 6' },
  { feature: 'Páginas de destino de áreas locales', starter: '—', growth: '—', pro: 'Hasta 6 (áreas de Houston)' },
  { feature: 'Optimización de Google Business Profile', starter: 'Incluido', growth: 'Incluido', pro: 'Incluido' },
  { feature: 'SEO local + marcado de esquema', starter: '—', growth: 'Incluido', pro: 'Incluido' },
  { feature: 'Optimización de búsqueda por voz', starter: '—', growth: 'Incluido', pro: 'Incluido' },
  { feature: 'Hacer clic para llamar y formularios de contacto', starter: '—', growth: 'Incluido', pro: 'Incluido' },
  { feature: 'Configuración de Google Analytics', starter: '—', growth: 'Incluido', pro: 'Incluido' },
  { feature: 'Creación de contenido personalizado', starter: '—', growth: '—', pro: 'Incluido' },
  { feature: 'Seguimiento avanzado de conversiones', starter: '—', growth: '—', pro: 'Incluido' },
  { feature: 'Análisis de la competencia + estrategia de palabras clave', starter: '—', growth: '—', pro: 'Incluido' },
  { feature: 'Optimización de 90 días + revisiones mensuales', starter: '—', growth: '—', pro: 'Incluido' }
];

// FAQ data
const faqs = [
  {
    id: 'need-website',
    question: '¿Necesito un sitio web primero?',
    answer: 'La mayoría de los clientes se benefician de tener un sitio web, pero Inicio se centra en Google Business Profile para ayudarle a recibir llamadas rápidamente.'
  },
  {
    id: 'setup-time',
    question: '¿Cuánto tiempo lleva la configuración?',
    answer: 'Inicio generalmente se completa en 1–2 semanas. Los sitios de Crecimiento a menudo se lanzan en 2–4 semanas. Los plazos de Pro varían según el alcance y el contenido.'
  },
  {
    id: 'contracts',
    question: '¿Hay contratos?',
    answer: 'Ofrecemos proyectos con alcance definido y opciones de soporte mes a mes. Los términos son claros y están documentados antes de comenzar el trabajo.'
  },
  {
    id: 'upgrade',
    question: '¿Puedo actualizar más tarde?',
    answer: 'Sí. Puede pasar de Inicio a Crecimiento o Pro a medida que se expandan sus necesidades. Trazaremos un camino práctico basado en objetivos y presupuesto.'
  },
  {
    id: 'reporting',
    question: '¿Qué informes recibo?',
    answer: 'Crecimiento y Pro incluyen configuración de análisis e informes. Para paquetes de SEO, recibirá información mensual y próximos pasos priorizados.'
  }
];
---

<MainLayout
  title="Precios Claros y Simples — Marketing AI Houston"
  description="Diseñado para Propietarios de Pequeñas Empresas. Tres planes: Inicio ($497), Crecimiento ($1,497), Pro ($3,497). Sin tarifas ocultas, sin contratos largos."
  currentPath="/es/pricing"
  breadcrumbs={breadcrumbs}
  keywords={['precios', 'precios de diseño web', 'precios de seo', 'costo de diseño web houston', 'costo de sitio web para pequeñas empresas', 'precios transparentes']}
  lang="es"
  alternateLanguages={alternateLanguages}
>
  <!-- Hero Section -->
  <section class="min-h-screen flex items-center justify-center relative overflow-hidden pt-16">
    <!-- Text background elements -->
    <div class="absolute inset-0 opacity-5 hidden md:block">
      <div class="text-9xl font-bold absolute top-10 left-10 text-blue-900">VALOR</div>
      <div class="text-9xl font-bold absolute top-1/2 right-10 text-teal-600">CLARO</div>
      <div class="text-9xl font-bold absolute bottom-10 left-1/3 text-blue-600">JUSTO</div>
    </div>

    <!-- Organic blob elements -->
    <div class="absolute inset-0">
      <div class="absolute top-20 left-20 w-64 h-64 bg-gradient-to-br from-blue-600 to-teal-600 opacity-20 animate-pulse" style="border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;"></div>
      <div class="absolute top-40 right-20 w-72 h-72 bg-gradient-to-br from-teal-600 to-blue-300 opacity-20 animate-pulse" style="border-radius: 20% 80% 20% 80% / 80% 20% 80% 20%; animation-delay: 0.4s;"></div>
      <div class="absolute bottom-20 left-1/3 w-80 h-80 bg-gradient-to-br from-blue-900 to-blue-600 opacity-20 animate-pulse" style="border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%; animation-delay: 0.8s;"></div>
    </div>

    <div class="relative z-10 px-4 max-w-6xl mx-auto">
      <div class="text-center">
        <!-- Stars and tagline -->
        <div class="text-yellow-400 text-3xl mb-2">⭐⭐⭐⭐⭐</div>
        <p class="text-gray-700 mb-8">Diseñado para Propietarios de Pequeñas Empresas</p>

        <!-- Headline -->
        <h1 class="text-5xl md:text-7xl font-black text-gray-900 leading-tight mb-6">
          Precios Claros y Simples
        </h1>

        <!-- Subheadline -->
        <p class="text-xl text-gray-700 mb-12 max-w-2xl mx-auto leading-relaxed">
          Nuestros planes están diseñados para crecer con usted — sin tarifas ocultas, sin contratos largos.
        </p>

        <!-- CTA Button -->
        <a
          href={`${import.meta.env.BASE_URL}es/contact#contact-options`}
          class="inline-block bg-gradient-to-r from-blue-600 to-teal-600 hover:from-blue-900 hover:to-blue-600 text-white font-semibold py-4 px-12 rounded-full transition-all duration-300 transform hover:scale-105 shadow-lg mb-6"
          aria-label="Reserve una GRATIS Consulta de Descubrimiento"
        >
          👉 Reserve una <span class="bg-yellow-400 text-black px-2 py-1 rounded font-bold">GRATIS</span> Consulta de Descubrimiento
        </a>

        <!-- Supporting one-liner -->
        <p class="text-gray-700 mb-12">Sin jerga, sin presión—solo una charla rápida sobre su negocio.</p>

        <!-- Key Metrics / Trust Cards -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12 max-w-5xl mx-auto">
          <div class="bg-white rounded-2xl p-6 shadow-lg text-center">
            <div class="text-4xl mb-3">💰</div>
            <div class="text-gray-900 font-bold">Precios Transparentes</div>
            <div class="text-gray-700 text-sm">Lo que cotizamos es lo que usted paga</div>
          </div>
          <div class="bg-white rounded-2xl p-6 shadow-lg text-center">
            <div class="text-4xl mb-3">📅</div>
            <div class="text-gray-900 font-bold">Sin Contratos Largos</div>
            <div class="text-gray-700 text-sm">Soporte mes a mes disponible</div>
          </div>
          <div class="bg-white rounded-2xl p-6 shadow-lg text-center">
            <div class="text-4xl mb-3">⚡</div>
            <div class="text-gray-900 font-bold">Proceso de Lanzamiento Rápido</div>
            <div class="text-gray-700 text-sm">Esté en línea en semanas, no meses</div>
          </div>
        </div>

        <!-- Industry Icons -->
        <div class="flex flex-wrap justify-center gap-8">
          <div class="flex flex-col items-center">
            <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg mb-3">
              <i class="fas fa-hammer text-2xl text-gray-900"></i>
            </div>
            <span class="text-gray-700 font-medium">Contratistas</span>
          </div>
          <div class="flex flex-col items-center">
            <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg mb-3">
              <i class="fas fa-car text-2xl text-gray-900"></i>
            </div>
            <span class="text-gray-700 font-medium">Talleres de Autos</span>
          </div>
          <div class="flex flex-col items-center">
            <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg mb-3">
              <i class="fas fa-broom text-2xl text-gray-900"></i>
            </div>
            <span class="text-gray-700 font-medium">Limpieza</span>
          </div>
          <div class="flex flex-col items-center">
            <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg mb-3">
              <i class="fas fa-leaf text-2xl text-gray-900"></i>
            </div>
            <span class="text-gray-700 font-medium">Jardinería</span>
          </div>
          <div class="flex flex-col items-center">
            <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg mb-3">
              <i class="fas fa-paw text-2xl text-gray-900"></i>
            </div>
            <span class="text-gray-700 font-medium">Servicios para Mascotas</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Pricing Cards -->
  <section class="py-20 md:py-24 relative overflow-hidden bg-gradient-to-br from-gray-50 to-white">
    <div class="container mx-auto max-w-7xl px-4 relative z-10">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12">
        {pricingPlans.map((plan) => (
          <div class={`relative rounded-2xl shadow-xl overflow-hidden transition-all duration-300 transform hover:-translate-y-2 ${
            plan.popular
              ? 'bg-gradient-to-br from-blue-900 to-blue-600 text-white scale-105 md:scale-110 border-4 border-blue-300'
              : plan.id === 'pro'
              ? 'bg-white border-2 border-blue-200'
              : 'bg-white border border-gray-200'
          }`}>
            {plan.popular && (
              <div class="bg-gradient-to-r from-teal-400 to-blue-300 py-2 px-4 text-center">
                <span class="text-blue-900 font-bold text-sm">MÁS POPULAR</span>
              </div>
            )}

            <div class="p-8">
              <div class="text-5xl mb-4">{plan.icon}</div>

              <h3 class={`text-2xl font-black mb-2 ${plan.popular ? 'text-white' : 'text-gray-900'}`}>
                {plan.name}
              </h3>

              <p class={`text-lg mb-4 font-semibold ${plan.popular ? 'text-blue-100' : 'text-blue-600'}`}>
                {plan.tagline}
              </p>

              <div class="mb-6">
                <span class={`text-5xl font-black ${plan.popular ? 'text-white' : 'text-blue-900'}`}>
                  {plan.price}
                </span>
              </div>

              <div class="mb-6">
                <p class={`font-bold mb-3 ${plan.popular ? 'text-blue-100' : 'text-gray-700'}`}>
                  Lo que usted obtiene:
                </p>
                <ul class="space-y-3">
                  {plan.features.map((feature) => (
                    <li class={`flex items-start gap-3 ${plan.popular ? 'text-blue-50' : 'text-gray-700'}`}>
                      <i class={`fas fa-check-circle mt-1 flex-shrink-0 ${plan.popular ? 'text-teal-300' : 'text-green-500'}`}></i>
                      <span class="text-sm">{feature}</span>
                    </li>
                  ))}
                </ul>
              </div>

              <p class={`text-sm mb-4 italic ${plan.popular ? 'text-blue-100' : 'text-gray-600'}`}>
                <strong>Mejor para:</strong> {plan.bestFor}
              </p>

              <a
                href={plan.learnMoreUrl}
                class={`block text-center text-sm mb-4 hover:underline transition-colors duration-200 ${
                  plan.popular ? 'text-blue-200 hover:text-white' : 'text-blue-600 hover:text-blue-800'
                }`}
                aria-label={`Más información sobre el plan ${plan.name}`}
              >
                Más información: {plan.learnMoreLabel} ›
              </a>

              <a
                href={plan.ctaUrl}
                class={`block text-center px-6 py-4 rounded-lg font-bold text-base transition-all duration-300 shadow-lg hover:shadow-xl hover:-translate-y-1 ${
                  plan.popular
                    ? 'bg-white text-blue-900 hover:bg-blue-50'
                    : 'bg-blue-600 text-white hover:bg-blue-700'
                }`}
                aria-label={`${plan.ctaLabel} para el plan ${plan.name}`}
              >
                👉 {plan.ctaLabel}
              </a>
            </div>
          </div>
        ))}
      </div>

      <!-- Custom and Monthly Options -->
      <div class="text-center max-w-3xl mx-auto">
        <div class="inline-flex items-center bg-gradient-to-r from-blue-50 to-teal-50 border-2 border-blue-200 rounded-full px-8 py-4 shadow-md hover:shadow-lg transition-all duration-300">
          <i class="fas fa-info-circle text-blue-600 mr-3 text-lg"></i>
          <p class="text-base font-semibold text-gray-800">
            <a
              href={`${import.meta.env.BASE_URL}es/contact`}
              class="text-blue-600 hover:text-blue-800 transition-colors font-bold"
              aria-label="Ver opciones de precios personalizados"
            >
              Precios personalizados
            </a>
            <span class="mx-2">y</span>
            <a
              href={`${import.meta.env.BASE_URL}es/seo-growth-strategy`}
              class="text-blue-600 hover:text-blue-800 transition-colors font-bold"
              aria-label="Explorar paquetes mensuales de SEO"
            >
              paquetes mensuales
            </a>
            <span class="ml-2">disponibles bajo pedido</span>
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Plan Comparison Table -->
  <section class="py-20 md:py-24 bg-white">
    <div class="container mx-auto max-w-6xl px-4">
      <h2 class="text-3xl md:text-5xl font-black text-center mb-12 text-gray-900">
        Compare Planes de un Vistazo
      </h2>

      <!-- Desktop Table -->
      <div class="hidden md:block overflow-x-auto">
        <table class="w-full border-collapse" role="table" aria-label="Compare planes de un vistazo">
          <caption class="sr-only">Comparación de los planes Inicio, Crecimiento y Pro</caption>
          <thead>
            <tr class="border-b-2 border-gray-300">
              <th scope="col" class="py-4 px-6 text-left text-gray-900 font-bold text-lg">Característica</th>
              <th scope="col" class="py-4 px-6 text-center text-gray-900 font-bold text-lg">Inicio</th>
              <th scope="col" class="py-4 px-6 text-center text-blue-900 font-bold text-lg bg-blue-50">Crecimiento ⭐</th>
              <th scope="col" class="py-4 px-6 text-center text-gray-900 font-bold text-lg">Pro</th>
            </tr>
          </thead>
          <tbody>
            {comparisonFeatures.map((row, index) => (
              <tr class={`border-b border-gray-200 ${index % 2 === 0 ? 'bg-gray-50' : 'bg-white'} hover:bg-blue-50 transition-colors`}>
                <td class="py-4 px-6 font-medium text-gray-900 text-sm">{row.feature}</td>
                <td class="py-4 px-6 text-center text-gray-700 text-sm">{row.starter}</td>
                <td class="py-4 px-6 text-center text-blue-900 bg-blue-50/50 text-sm font-medium">{row.growth}</td>
                <td class="py-4 px-6 text-center text-gray-700 text-sm">{row.pro}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <!-- Mobile Stacked Cards -->
      <div class="md:hidden space-y-6">
        {pricingPlans.map((plan) => (
          <div class={`rounded-xl overflow-hidden shadow-lg ${plan.popular ? 'border-2 border-blue-600' : 'border border-gray-200'}`}>
            <div class={`py-3 px-4 font-bold text-center ${plan.popular ? 'bg-blue-600 text-white' : 'bg-gray-100 text-gray-900'}`}>
              {plan.name} {plan.popular ? '⭐' : ''}
            </div>
            <div class="p-4 bg-white space-y-3">
              {comparisonFeatures.map((row) => (
                <div class="flex justify-between items-start py-2 border-b border-gray-100 last:border-0">
                  <span class="text-xs text-gray-700 font-medium pr-2">{row.feature}</span>
                  <span class={`text-xs font-semibold ${plan.id === 'starter' ? 'text-gray-900' : plan.id === 'growth' ? 'text-blue-900' : 'text-gray-900'}`}>
                    {plan.id === 'starter' ? row.starter : plan.id === 'growth' ? row.growth : row.pro}
                  </span>
                </div>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- What's Included Details -->
  <section class="py-20 md:py-24 bg-gradient-to-br from-blue-50 to-white">
    <div class="container mx-auto max-w-6xl px-4">
      <h2 class="text-3xl md:text-5xl font-black text-center mb-16 text-gray-900">
        Qué Está Incluido
      </h2>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <!-- Starter Essentials -->
        <div class="bg-white/80 backdrop-blur-sm rounded-xl p-8 shadow-lg border border-white/50">
          <div class="text-5xl mb-4">💡</div>
          <h3 class="text-2xl font-black text-gray-900 mb-4">Elementos Esenciales de Inicio</h3>

          <div class="space-y-4 mb-6">
            <div>
              <p class="font-bold text-gray-900 text-sm mb-1">Google Business Profile:</p>
              <p class="text-sm text-gray-700">Configuración, optimización, categorías, fotos</p>
            </div>

            <div>
              <p class="font-bold text-gray-900 text-sm mb-1">Reseñas y mensajería:</p>
              <p class="text-sm text-gray-700">Plantillas, capacitación, configuración de respuesta automática</p>
            </div>

            <div>
              <p class="font-bold text-blue-600 text-sm">Enfoque de resultados:</p>
              <p class="text-sm text-gray-700 italic">Aparezca localmente y comience a generar llamadas</p>
            </div>
          </div>
        </div>

        <!-- Growth Foundations -->
        <div class="bg-gradient-to-br from-blue-900 to-blue-600 rounded-xl p-8 shadow-xl text-white transform scale-105">
          <div class="text-5xl mb-4">🚀</div>
          <h3 class="text-2xl font-black mb-4">Fundamentos de Crecimiento</h3>

          <div class="space-y-4 mb-6">
            <div>
              <p class="font-bold text-blue-100 text-sm mb-1">Construcción del sitio web:</p>
              <p class="text-sm text-blue-50">4 páginas optimizadas para móviles, diseño responsivo</p>
            </div>

            <div>
              <p class="font-bold text-blue-100 text-sm mb-1">Preparación para SEO:</p>
              <p class="text-sm text-blue-50">SEO local, esquema, búsqueda por voz</p>
            </div>

            <div>
              <p class="font-bold text-blue-100 text-sm mb-1">Configuración de conversión:</p>
              <p class="text-sm text-blue-50">Hacer clic para llamar, formularios, análisis</p>
            </div>

            <div>
              <p class="font-bold text-teal-300 text-sm">Enfoque de resultados:</p>
              <p class="text-sm text-blue-100 italic">Convierta visitas en clientes potenciales con una base clara</p>
            </div>
          </div>
        </div>

        <!-- Pro Expansion -->
        <div class="bg-white/80 backdrop-blur-sm rounded-xl p-8 shadow-lg border-2 border-blue-200">
          <div class="text-5xl mb-4">🌐</div>
          <h3 class="text-2xl font-black text-gray-900 mb-4">Expansión Pro</h3>

          <div class="space-y-4 mb-6">
            <div>
              <p class="font-bold text-gray-900 text-sm mb-1">Escalar contenido:</p>
              <p class="text-sm text-gray-700">Hasta 16 páginas, páginas de servicios, páginas de destino locales</p>
            </div>

            <div>
              <p class="font-bold text-gray-900 text-sm mb-1">Estrategia y seguimiento:</p>
              <p class="text-sm text-gray-700">Contenido personalizado, seguimiento avanzado, competencia + palabras clave</p>
            </div>

            <div>
              <p class="font-bold text-gray-900 text-sm mb-1">Optimización:</p>
              <p class="text-sm text-gray-700">Plan de 90 días con revisiones mensuales</p>
            </div>

            <div>
              <p class="font-bold text-blue-600 text-sm">Enfoque de resultados:</p>
              <p class="text-sm text-gray-700 italic">Expanda la visibilidad, fortalezca el compromiso, sostenga el crecimiento</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Our Commitments -->
  <section class="py-12 bg-white">
    <div class="container mx-auto max-w-5xl px-4">
      <div class="flex flex-col md:flex-row justify-center items-center gap-4 md:gap-6">
        <div class="w-full md:w-auto bg-green-50 rounded-lg shadow-sm px-4 py-3 flex items-center gap-3 border border-green-100 hover:shadow-md transition-shadow">
          <i class="fas fa-check-circle text-green-600 text-xl flex-shrink-0"></i>
          <span class="text-gray-800 text-sm font-medium"><strong>Sin tarifas ocultas:</strong> Lo que cotizamos es lo que usted paga.</span>
        </div>

        <div class="w-full md:w-auto bg-green-50 rounded-lg shadow-sm px-4 py-3 flex items-center gap-3 border border-green-100 hover:shadow-md transition-shadow">
          <i class="fas fa-check-circle text-green-600 text-xl flex-shrink-0"></i>
          <span class="text-gray-800 text-sm font-medium"><strong>Sin contratos largos:</strong> Soporte mes a mes disponible.</span>
        </div>

        <div class="w-full md:w-auto bg-green-50 rounded-lg shadow-sm px-4 py-3 flex items-center gap-3 border border-green-100 hover:shadow-md transition-shadow">
          <i class="fas fa-check-circle text-green-600 text-xl flex-shrink-0"></i>
          <span class="text-gray-800 text-sm font-medium"><strong>Precios transparentes:</strong> Planes diseñados para escalar con su negocio.</span>
        </div>
      </div>

      <p class="text-center text-xs text-gray-500 mt-6">
        Consulte la página de Precios para conocer los términos completos y los planes disponibles.
      </p>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="py-20 md:py-24 bg-gradient-to-br from-gray-50 to-white">
    <div class="container mx-auto max-w-4xl px-4">
      <h2 class="text-3xl md:text-5xl font-black text-center mb-4 text-gray-900">
        Preguntas Frecuentes
      </h2>
      <p class="text-xl text-gray-700 max-w-2xl mx-auto text-center mb-12">Respuestas directas — sin jerga, sin letra pequeña oculta.</p>

      <div class="space-y-4 max-w-3xl mx-auto">
        {faqs.map((faq) => (
          <div class="bg-white rounded-xl overflow-hidden border border-gray-200 shadow-md">
            <button
              class="accordion-trigger w-full text-left p-6 hover:bg-blue-50/30 focus:outline-none focus:ring-2 focus:ring-blue-300 flex justify-between items-center transition-colors"
              aria-expanded="false"
              aria-controls={`faq-${faq.id}`}
            >
              <span class="font-bold text-blue-900 pr-4 text-base md:text-lg flex items-center">
                <span class="mr-3 text-2xl">
                  {faq.id === 'need-website' ? '🌐' : faq.id === 'setup-time' ? '⏱️' : faq.id === 'contracts' ? '📋' : faq.id === 'upgrade' ? '🔄' : '📊'}
                </span>
                <span>{faq.question}</span>
              </span>
              <i class="fas fa-chevron-down text-blue-600 transition-transform duration-300 flex-shrink-0 accordion-icon"></i>
            </button>
            <div id={`faq-${faq.id}`} class="accordion-content overflow-hidden" style="max-height: 0;" aria-hidden="true">
              <div class="p-6 pt-0 text-gray-700 text-sm md:text-base">
                {faq.answer}
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Final CTA Section -->
  <section class="py-20 md:py-24 bg-gradient-to-br from-blue-900 to-blue-600 text-white">
    <div class="container mx-auto max-w-4xl px-4 text-center">
      <h2 class="text-4xl md:text-5xl font-black mb-6">
        ¿Listo para Comenzar?
      </h2>

      <p class="text-xl md:text-2xl text-blue-100 mb-10 max-w-2xl mx-auto">
        Programe una consulta de descubrimiento gratuita y trazaremos sus primeros pasos.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a
          href={`${import.meta.env.BASE_URL}es/contact#contact-options`}
          class="inline-block bg-white text-blue-900 px-8 py-4 rounded-lg font-bold text-lg hover:bg-blue-50 transition-all duration-300 shadow-xl hover:shadow-2xl hover:-translate-y-1 w-full sm:w-auto"
          aria-label="Reserve una GRATIS Consulta de Descubrimiento"
        >
          👉 Reserve una GRATIS Consulta de Descubrimiento
        </a>

        <a
          href={`${import.meta.env.BASE_URL}es/contact#contact-form`}
          class="inline-block bg-blue-700 text-white px-8 py-4 rounded-lg font-bold text-lg hover:bg-blue-800 border-2 border-white/30 transition-all duration-300 shadow-xl hover:shadow-2xl hover:-translate-y-1 w-full sm:w-auto"
          aria-label="Solicite una Propuesta Personalizada"
        >
          📩 Solicite una Propuesta Personalizada
        </a>
      </div>
    </div>
  </section>

  <!-- FAQ Accordion Script -->
  <script>
    // Wait for DOM
    document.addEventListener('DOMContentLoaded', function() {
      // FAQ Accordion functionality
      const accordionTriggers = document.querySelectorAll('.accordion-trigger');

      accordionTriggers.forEach(function(trigger) {
        trigger.addEventListener('click', function() {
          const isOpen = trigger.getAttribute('aria-expanded') === 'true';
          const contentId = trigger.getAttribute('aria-controls');
          const content = document.getElementById(contentId || '');
          const icon = trigger.querySelector('.accordion-icon');

          // Update state FIRST
          const newState = !isOpen;

          // Close all other accordions
          accordionTriggers.forEach(function(otherTrigger) {
            if (otherTrigger !== trigger) {
              var otherContentId = otherTrigger.getAttribute('aria-controls');
              var otherContent = document.getElementById(otherContentId || '');
              var otherIcon = otherTrigger.querySelector('.accordion-icon');

              otherTrigger.setAttribute('aria-expanded', 'false');
              if (otherContent) {
                otherContent.setAttribute('aria-hidden', 'true');
                otherContent.style.maxHeight = '0';
              }
              if (otherIcon) {
                otherIcon.classList.remove('rotate-180');
              }
            }
          });

          // Toggle current accordion
          trigger.setAttribute('aria-expanded', newState ? 'true' : 'false');
          if (content) {
            content.setAttribute('aria-hidden', newState ? 'false' : 'true');

            if (newState) {
              content.style.maxHeight = content.scrollHeight + 'px';
              if (icon) {
                icon.classList.add('rotate-180');
              }
            } else {
              content.style.maxHeight = '0';
              if (icon) {
                icon.classList.remove('rotate-180');
              }
            }
          }
        });
      });
    });
  </script>
</MainLayout>
